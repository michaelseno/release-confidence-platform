"""Reserved identifier constants for future evidence, metadata, and logs."""

MANDATORY_IDENTIFIERS: tuple[str, ...] = (
    "client_id",
    "audit_id",
    "run_id",
    "endpoint_id",
    "scenario_id",
    "raw_result_version",
)
