Resources: {}
Outputs:
  BackendLambdaIamScope:
    Description: Backend Lambda role is scoped to stage S3, DynamoDB, Secrets Manager, and CloudWatch Logs resources in serverless.yml.
    Value: stage-scoped-runtime-permissions
